'use strict';

module.exports = {
  up: (queryInterface, Sequelize) => {
    // return queryInterface.addColumn(
    //   'dibs_studios',
    //   'offboard_at',
    //   Sequelize.DATE,
    // );
  },

  down: (queryInterface, Sequelize) => {
    // return queryInterface.removeColumn('dibs_studios', 'offboard_at');
  },
};
