// material-ui
import { Grid, Typography } from '@mui/material';

// project imports

// ==============================|| STUDIO ADMIN -> PAYOUTS -> GENERAL ||============================== //

const GeneralPayoutsPage = () => (
    <Grid container direction="column">
        <Grid item xs={5}>
            <Typography gutterBottom variant="h4">
                Payouts are here
            </Typography>
        </Grid>
    </Grid>
);

export default GeneralPayoutsPage;
